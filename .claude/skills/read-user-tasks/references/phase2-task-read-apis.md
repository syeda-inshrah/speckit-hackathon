# Phase 2 References – Task Read Operations

This skill must rely on existing Phase 2 infrastructure.

Allowed integrations:
- CRUD Agent (read operations)
- Backend API Agent (GET /tasks)
- Authentication middleware

Rules:
- Do NOT create new endpoints
- Do NOT access the database directly
- Do NOT bypass permission checks
